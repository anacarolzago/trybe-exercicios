/*
    Função summation
    - Deve receber um array contendo números inteiros
    - Deve retornar o somatório (valor numérico inteiros) de todos os itens do array (teste)
    - Se receber um array vazio, retorna throw com mensagem 'Array não pode ser vazio' (teste)
*/

const summation = (array) => {};
module.exports = summation;