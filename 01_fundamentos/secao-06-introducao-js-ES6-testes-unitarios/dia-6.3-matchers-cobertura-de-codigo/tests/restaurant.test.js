const {addDiscount, purchaseData} = require('../src/restaurant');

describe('Test restaurant', () => {
  it.todo('Verificar se após chamar a addDiscount, haverá uma KEY com nome discount dentro de purchaseData');
});