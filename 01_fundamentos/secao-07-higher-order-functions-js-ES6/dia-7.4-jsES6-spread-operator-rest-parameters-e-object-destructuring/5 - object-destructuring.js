const darkKnight = {
    name: 'Bruce Wayne',
    city: 'Gotham',
    superPowers: 'milionário',
    sideKicks: ['Robin', 'BatGirl', 'Alfred']
}

const { name: nome, city } = darkKnight;
console.log(`${nome}  ${city}`);